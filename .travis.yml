language: python

python:
  - "3.6"

branches:
  only:
  - master
  - development

install:
  - pip install -r requirements.txt
  - python3 setup.py install

script:
  py.test --cov=mgcpy

after_success:
  - coveralls
